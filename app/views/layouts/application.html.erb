<!DOCTYPE html>
<html>
<head>
  <title>LavenderBlush</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body class =" <%= "home_page" if session[:user_id] == nil %> ">

  <% if session[:user_id] != nil %>
  <!-- Navigation -->
  <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">

        <!-- Brand and toggle get grouped for better mobile display  -->
        <div class="navbar-header">
          <span id="cat"><%= image_tag "lavender_blush_copy2.png", width: "80px", height: "60px" %></span>
          <%= link_to "<h2>LavenderBlush</h2>".html_safe, root_path, class: "navbar-brand" %>
          <%= link_to "My Account", user_path(current_user.id), class: "navbar-brand" %>
          <%= button_to "#{User.find(session[:user_id]).name} Sign Out", :logout, class: "btn btn-default" %>
        </div>
<!-- ##################### -->
  <div id="search-bar" >
    <span>Search With: </span>

      <img src="twitter.png" alt="" class = "twitter-bird img-circle" >

      <img src="vimeo.png" alt="" class = "vimeo-v img-circle" >


    </div>
      </nav>
    <div id="search-fields">
      <div id="twitter-search" class = "">
        <%= render "shared/twitter_search" %>
      </div>
      <div id="vimeo-search" class = "">
        <%= render "shared/vimeo_search" %>
      </div>
    </div>


  <% end %>

  <!-- Page Content -->
  <div class="container">

    <div class="row">
      <div class="col-lg-12 text-center">

        <%= yield %>
      </div>
    </div>
    <!-- /.row -->

  </div>
  <!-- /.container -->

</body>
</html>
